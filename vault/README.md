# Encrypted source vault

This directory is reserved for encrypted source snapshots and manifests. Decrypted files and encryption keys are ignored by Git and must never be committed.

The initial repository contains no original medical documents, photos, videos, or full ChatGPT export.
